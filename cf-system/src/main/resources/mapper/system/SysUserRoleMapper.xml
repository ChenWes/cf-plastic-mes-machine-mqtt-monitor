<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cf.system.mapper.SysUserRoleMapper">

	<resultMap type="SysUserRole" id="SysUserRoleResult">
		<result property="userId"     column="user_id"      />
		<result property="roleId"     column="role_id"      />
		<result property="userRoleId"     column="user_role_id"      />
		<result property="createBy"           column="create_by"             />
		<result property="createTime"         column="create_time"           />
		<result property="updateBy"           column="update_by"             />
		<result property="updateTime"         column="update_time"           />

	</resultMap>

	<select id="getUserRoleByRoleId" parameterType="Long" resultMap="SysUserRoleResult" >
		SELECT `sys_user_role`.`user_role_id`,
		`sys_user_role`.`user_id`,
		`sys_user_role`.`role_id`,
		`sys_user_role`.`create_by`,
		`sys_user_role`.`create_time`,
		`sys_user_role`.`update_by`,
		`sys_user_role`.`update_time`
		FROM  `sys_user_role` where role_id=#{roleId}
	</select>

	<delete id="deleteUserRoleByUserId" parameterType="Long">
		delete from sys_user_role where user_id=#{userId}
	</delete>

	<delete id="deleteUserRoleByRoleId" parameterType="Long">
		delete from sys_user_role where role_id=#{roleId}
	</delete>

	<delete id="deleteUserRoleById" parameterType="Long">
		delete from sys_user_role where user_role_id=#{userRoleId}
	</delete>

	
	<insert id="batchUserRole">
		insert into sys_user_role(user_id, role_id,create_by,create_time) values
		<foreach item="item" index="index" collection="list" separator=",">
			(#{item.userId},#{item.roleId},#{item.createBy},#{item.createTime})
		</foreach>
	</insert>
	
	<delete id="deleteUserRoleInfo" parameterType="SysUserRole">
		delete from sys_user_role where user_id=#{userId} and role_id=#{roleId}
	</delete>


</mapper> 