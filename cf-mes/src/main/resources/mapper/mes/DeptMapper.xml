<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cf.mes.mapper.DeptMapper">
    
    <resultMap type="Dept" id="DeptResult">
        <result property="deptId"    column="dept_id"    />
        <result property="deptCode"    column="dept_code"    />
        <result property="deptName"    column="dept_name"    />
        <result property="status"    column="status"    />
        <result property="createBy"    column="create_by"    />
        <result property="createTime"    column="create_time"    />
        <result property="updateBy"    column="update_by"    />
        <result property="updateTime"    column="update_time"    />
        <result property="remark"    column="remark"    />
    </resultMap>

    <sql id="getDeptSql">
        select dept_id, dept_code, dept_name, status, create_by, create_time, update_by, update_time,remark from dept
    </sql>

    <select id="getDeptList" parameterType="Dept" resultMap="DeptResult">
        <include refid="getDeptSql"/>
        <where>  
            <if test="deptCode != null  and deptCode != ''"> and dept_code like concat('%', #{deptCode}, '%')</if>
            <if test="deptName != null  and deptName != ''"> and (dept_name like concat('%', #{deptName}, '%') or dept_id in (select record_id from sys_lang_info where table_name='dept' and column_name='dept_name' and value like concat('%', #{deptName}, '%')))</if>
            <if test="status != null  and status != ''"> and status = #{status}</if>
            <if test="remark != null  and remark != ''"> and remark like concat('%', #{remark}, '%')</if>
        </where>
    </select>
    
    <select id="getDeptById" parameterType="Long" resultMap="DeptResult">
          <include refid="getDeptSql"/>
        where dept_id = #{deptId}
    </select>

    <select id="getDeptByCode" parameterType="String" resultMap="DeptResult">
        <include refid="getDeptSql"/>
        where dept_code = #{deptCode}
    </select>
        
    <insert id="insertDept" parameterType="Dept" useGeneratedKeys="true" keyProperty="deptId">
        insert into dept
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="deptCode != null and deptCode != ''">dept_code,</if>
            <if test="deptName != null">dept_name,</if>
            <if test="status != null">status,</if>
            <if test="createBy != null">create_by,</if>
            <if test="createTime != null">create_time,</if>
            <if test="updateBy != null">update_by,</if>
            <if test="updateTime != null">update_time,</if>
            <if test="remark != null">remark,</if>
         </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="deptCode != null and deptCode != ''">#{deptCode},</if>
            <if test="deptName != null">#{deptName},</if>
            <if test="status != null">#{status},</if>
            <if test="createBy != null">#{createBy},</if>
            <if test="createTime != null">#{createTime},</if>
            <if test="updateBy != null">#{updateBy},</if>
            <if test="updateTime != null">#{updateTime},</if>
            <if test="remark != null">#{remark},</if>
         </trim>
    </insert>

    <update id="updateDept" parameterType="Dept">
        update dept
        <trim prefix="SET" suffixOverrides=",">
            <if test="deptName != null">dept_name = #{deptName},</if>
            <if test="status != null">status = #{status},</if>
            <if test="createBy != null">create_by = #{createBy},</if>
            <if test="createTime != null">create_time = #{createTime},</if>
            <if test="updateBy != null">update_by = #{updateBy},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="remark != null">remark = #{remark},</if>
        </trim>
        where dept_id = #{deptId}
    </update>

    <delete id="deleteDeptById" parameterType="Long">
        delete from dept where dept_id = #{deptId}
    </delete>

    <delete id="deleteDeptByIds" parameterType="String">
        delete from dept where dept_id in 
        <foreach item="deptId" collection="array" open="(" separator="," close=")">
            #{deptId}
        </foreach>
    </delete>
    
</mapper>